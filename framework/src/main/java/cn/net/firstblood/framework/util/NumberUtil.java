package cn.net.firstblood.framework.util;import java.text.DecimalFormat;/** * 数字工具 * @author gangxiang.chengx * */public class NumberUtil {	public static final String DEFAUlt_fromat = "###.##";    /**     * 格式化数字     * @return     */    public static String format(Object srcNumber, String format) {        // 做好容错，防止空指针异常        if (null == srcNumber){            srcNumber = 0;        }    	DecimalFormat df = new DecimalFormat(format);    	return df.format(srcNumber);    }        /**     * 格式化数字     * @return     */    public static String format(Object srcNumber) {        // 做好容错，防止空指针异常        if (null == srcNumber){            srcNumber = 0;        }    	DecimalFormat df = new DecimalFormat(DEFAUlt_fromat);    	return df.format(srcNumber);    }}